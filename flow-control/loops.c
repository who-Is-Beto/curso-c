#include <stdio.h>

int main()
{

  int number = 1, count = 10;

  while (count > 0)
  {
    printf("the count is %i \n", count);
    count--;
  }

  for (int i = 0; i < 10; i++)
  {
    printf("i is equals to %i \n", i);
  }

  return 0;
}